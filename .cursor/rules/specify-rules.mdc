---
alwaysApply: true
---

<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan at
`specs/001-migrate-salesforce-customizations/plan.md` and `quickstart.md`.

Dataverse work MUST follow skills under `.github/plugins/dataverse/skills/`
(dv-connect, dv-metadata, dv-solution, dv-query, dv-overview). Salesforce
source is a multi-entity catalog in `src/Entity/{EntityPackage}/` (Account is
first; more folders will be added). Migration tooling in `migration/` uses
`entity-registry.json` and per-entity `maps/*-field-map.json`; solution output
in `solutions/AccountMigration/`.
<!-- SPECKIT END -->
